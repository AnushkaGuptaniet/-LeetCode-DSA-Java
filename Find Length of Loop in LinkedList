import java.util.HashSet;
public class findLengthOfLoop {
    static class Node{
        int data;
        Node next;
        Node(int data){
            this.data = data;
            this.next = null;
        }
    }
    public static void main(String[] args) {
        Node a = new Node(1);
        Node b = new Node(2);
        Node c = new Node(3);
        Node d = new Node(4);
        Node e = new Node(5);
        a.next = b;
        b.next = c;
        c.next = d;
        d.next = e;
        e.next = b;
        Node head = a;
        int pos = 1;
        System.out.println(findLengthofLoop(head));

    }

    // brute force
    public static int findLengthofLoop(Node head){
        Node temp = head;
        HashSet<Node> set = new HashSet<>();
        while(temp!=null){
            if(set.contains(temp)){
                return countLength(temp);
            }
            else{
                set.add(temp);
                temp = temp.next;
            }
        }
        return 0;
    }

    // TC: O(N), SC: O(n)


    // optimized approach
//    public static int findLengthofLoop(Node head){
//        if(head == null){
//            return 0;
//        }
//        Node slow = head;
//        Node fast = head;
//        while(fast != null && fast.next != null){
//            slow = slow.next;
//            fast = fast.next.next;
//            if(slow == fast){
//                return countLength(slow);
//            }
//        }
//        return 0;
//    }
    // TC: O(N), SC: O(1)
    public static int countLength(Node start){
        int count = 1;
        Node temp = start.next;
        while(temp != start){
            count++;
            temp = temp.next;
        }
        return count;
    }

}
